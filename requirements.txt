# === Основное ===
aiogram==3.16.0
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1
matplotlib

# === БД и миграции ===
sqlalchemy[asyncio]==2.0.35
asyncpg==0.29.0          # для PostgreSQL (Railway/Render)
aiosqlite==0.20.0        # для локального sqlite

# === Google Sheets (лиды) ===
gspread==6.1.2
oauth2client==4.1.3
google-auth==2.35.0
google-auth-oauthlib==1.2.1

# === Рассылки и отложенные задачи ===
APScheduler==3.10.4

# === Логи, шаблоны, утилиты ===
loguru==0.7.2
jinja2==3.1.4
aiofiles==24.1.0

# === QR-коды для оплаты, крипта ===
qrcode[pil]==7.4.2
cryptg==0.5.0
yookassa

# === Опционально, но пригодится позже ===
redis==5.0.8             # для FSM-хранилища на проде
aioredis==2.0.1          # если захотим Redis